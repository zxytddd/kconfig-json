conf.c: 162 - 381 为原先的3个函数，这三个函数用来修改config中的值

后面是我改写的这三个函数，对于整个config内容进行索引，并且分别和JSON中的值进行对比，当匹配后，进行修改。所有的依赖关系由调用的KConfig函数解决。

known issues:
对于JSON文件的查找非常简单。
修改值时未对值的类型进行检查。
如果某个选项为不可见，那么对他的修改讲不起作用。
